# Contributing

This is a personal scaffolding repo. Before committing:

- run `gofmt`
- build `cproj`
- generate at least one smoke project
- build and test the generated smoke project
- keep generated defaults aligned with `docs/c-style-guide.md`

Avoid adding clever generator behavior until at least one real project needs it.
